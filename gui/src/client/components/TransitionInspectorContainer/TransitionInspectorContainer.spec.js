import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import TransitionInspectorContainer from '.';

describe('<TransitionInspectorContainer />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <TransitionInspectorContainer />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<TransitionInspectorContainer className="test-class-name" />);
    expect(wrapper).to.have.className('transition-inspector-container');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
