import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import InspectorContainer from '.';

describe('<InspectorContainer />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <InspectorContainer />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<InspectorContainer className="test-class-name" />);
    expect(wrapper).to.have.className('inspector-container');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
