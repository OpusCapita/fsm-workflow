import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import StateNodeInspector from '.';

describe('<StateNodeInspector />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <StateNodeInspector />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<StateNodeInspector className="test-class-name" />);
    expect(wrapper).to.have.className('state-node-inspector');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
