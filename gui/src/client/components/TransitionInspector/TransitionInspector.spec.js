import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import TransitionInspector from '.';

describe('<TransitionInspector />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <TransitionInspector />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<TransitionInspector className="test-class-name" />);
    expect(wrapper).to.have.className('transition-inspector');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
